import { Network } from "./network";
interface ModifiedUserData {
    username?: string;
    password?: {
        OldPassword: string;
        NewPassword: string;
    };
    email?: string;
}
/**
 * Interface for batching edits together; NOT DONE
 */
export declare class Editor {
    private _network;
    _modified: ModifiedUserData;
    setEmail(email: string): this;
    setUsername(username: string): this;
    setPassword(oldpassword: string, password: string): this;
    save(): Promise<true | undefined>;
    constructor(_network: Network);
}
export {};
