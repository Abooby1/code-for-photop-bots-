const NameVar = {
  names: ["commandname"],
  func: ({chat})=>{
    command use
  },
  description: "command description"
  permission: "rank permission to use the command (optional)"
};

export {NameVar};