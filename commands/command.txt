const NameVar = {
  names: ["commandname"],
  func: ({chat})=>{ //can add: userData (for data), body (the text after the command), args: [] (args....), post (for post.chat and other post things), and client (for client.post and other client things)
    command use
  },
  description: "command description",
  permission: "rank permission to use the command (optional)"
};

export {NameVar};